import {motion} from "framer-motion"
import Image from 'next/image'
import { Children, useState } from "react";

const Card = ({children}) => {

    return(
        <h1>{children}</h1>
    )
    //Nothing here for now just a setup
}